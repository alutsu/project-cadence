/**
 * Headless entry point for the balance harness (GDD §19, CLAUDE.md §7.3).
 * S0 ships the wiring only; S1 gives it a timeline to print.
 */
function main(): void {
  process.stdout.write('cadence sim harness — no simulation yet (M0/S1)\n');
}

main();
